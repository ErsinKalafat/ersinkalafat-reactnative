export interface IProduct {
    id: number
    name: string
    developerEmail: string
    description: string
    category: string
    price: number
    avatar: string,
    createdAt: string
}

export interface ICategories {
    id: number
    name: string
    createdAt: string
}